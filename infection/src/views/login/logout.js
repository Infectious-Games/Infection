import React from 'react';
import { Button } from 'react-bootstrap';

const Logout = () => (
  // <Button href="/logout" bsStyle="info" bsSize="small" active>
  <Button href="/" bsStyle="info" bsSize="small" active>
    Logout
  </Button>
);

export default Logout;
