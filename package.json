{
  "engines": {
    "node": "8.11.1"
  },
  "scripts": {
    "preinstall": "cd infection && yarn && yarn build",
    "start": "cd infection && yarn start"
  },
  "dependencies": {
    "yarn": "^1.22.0"
  }
}
